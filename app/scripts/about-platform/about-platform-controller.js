'use strict';

angular.module('dmc.about-platform')
    .controller('about-platform-controller', ['$stateParams', '$state', "$scope", "ajax", function ($stateParams, $state, $scope, ajax) {

    }]
);
