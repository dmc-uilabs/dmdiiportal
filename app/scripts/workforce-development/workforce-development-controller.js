'use strict';

angular.module('dmc.workforce-development')
    .controller('workforce-development-controller', ['$stateParams', '$state', "$scope", "ajax", function ($stateParams, $state, $scope, ajax) {

    }]
);
