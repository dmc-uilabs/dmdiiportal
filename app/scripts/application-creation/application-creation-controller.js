'use strict';

angular.module('dmc.application-creation')
    .controller('application-creation-controller', ['$stateParams', '$state', "$scope", "ajax", function ($stateParams, $state, $scope, ajax) {

    }]
);
